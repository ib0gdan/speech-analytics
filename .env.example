# Copy to .env and fill in. .env is gitignored — never commit real keys.

# Groq — OpenAI-compatible LLM backend for the agents. Free key: https://console.groq.com
GROQ_API_KEY=gsk_replace_me
LLM_MODEL=llama-3.3-70b-versatile

# OpenWebUI Pipelines server auth (default from the official docs; fine for local).
PIPELINES_API_KEY=0p3n-w3bu!
