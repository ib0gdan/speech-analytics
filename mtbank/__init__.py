"""MTBank Call Analytics — shared, framework-agnostic core.

Imported by BOTH the OpenWebUI Pipeline (pipelines/mtbank_pipeline.py) and the FastAPI
service (api/main.py). Single source of truth for analysis logic — no duplication (CORE-01).
"""

__version__ = "0.1.0-skeleton"
