# Auto-loads deploy/grafana/dashboards/mtbank.json on startup — zero manual clicking.
apiVersion: 1

providers:
  - name: mtbank
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
