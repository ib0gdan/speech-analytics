[pytest]
testpaths = tests
addopts = -q --strict-markers
markers =
    slow: loads the real faster-whisper model. Runs by default (it is the integration test);
        deselect with `pytest -m "not slow"` when you want the fast, network-free suite.
